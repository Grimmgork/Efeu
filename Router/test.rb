# Name of behaviour

on NameOfMessage { 
		id: 10 
	} do
	emit Message
  emit Message
  await Message { 
		id: 10 
		name: 10 
	} do
    let name be (a: b: c: a / b)
    let name be (if (if 10, (a: b: name), 30), (split "name", "asdf"), 
			(/namee/, "name").("adadw").name, 30)
		# 
		# some commments
    if a is 10 do
      emit Message
    else
      emit Message
    end
		for items do
			emit Message
			emit Message
			emit Message
			call Message {
				id: 10 
				name: 10
				name: 10
			} do
				await Message
			end
			await Message do
			end
		end
    await Message { 
			id: 10 
		} do
      emit Message
			emit Message
			emit Message
    end
	end
end

emit Message
emit Message
await Message { 
	id: 10 
	name: 10 
} do
	let name be 10
	let name be 20
	if a is 10 do
		emit
		emit
	end
end

# Scope

# Instance

# Definition

# scope ids:
#
# 2
# 2.1
# 2.2.0