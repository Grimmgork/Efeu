@using Efeu.Integration.Entities
@using Efeu.Runtime
@model BehaviourDefinitionVersionEntity;

@foreach (BehaviourDefinitionStep step in Model.Steps)
{
    <partial name="BehaviourDefinitionStep" model="@step" />
}
